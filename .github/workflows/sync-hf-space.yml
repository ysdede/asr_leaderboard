name: Sync to HF Space

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'app.config.*'
      - 'package.json'
      - 'package-lock.json'
      - 'tailwind.config.*'
      - 'postcss.config.*'
      - '.github/workflows/sync-hf-space.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build

      - name: Prepare HF export
        run: |
          # Pre-fetch CSV and embed as <script> into index.html
          CSV_URL="https://huggingface.co/datasets/ysdede/asr_benchmark_store/resolve/dev/metrics-00.csv"
          CSV_JSON=$(curl -sSfL "$CSV_URL" | python3 -c "
          import csv, json, sys
          reader = csv.DictReader(sys.stdin)
          print(json.dumps([row for row in reader]))
          " 2>/dev/null || echo '[]')

          mkdir -p hf_export
          sed -i "s|</body>|<script>window.__LEADERBOARD_DATA__=${CSV_JSON};</script></body>|" .output/public/index.html
          cp .output/public/index.html hf_export/
          cp -r .output/public/_build hf_export/ 2>/dev/null || true
          cp -r .output/public/assets hf_export/ 2>/dev/null || true
          cat > hf_export/README.md << 'EOF'
          ---
          title: Turkish Asr Leaderboard
          emoji: 🔥
          colorFrom: pink
          colorTo: blue
          sdk: static
          app_file: index.html
          pinned: false
          custom_headers:
            cross-origin-embedder-policy: credentialless
            cross-origin-opener-policy: same-origin
            cross-origin-resource-policy: cross-origin
          ---
          
          # Turkish ASR Leaderboard
          
          Interactive benchmark leaderboard for Turkish ASR models.
          Compares WER, CER, cosine similarity and speed across multiple datasets.
          
          Source: [ysdede/asr_leaderboard](https://github.com/ysdede/asr_leaderboard)
          EOF

      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - run: pip install -q huggingface_hub

      - name: Upload to HF Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          hf upload ysdede/turkish_asr_leaderboard hf_export . \
            --repo-type space \
            --delete '*' \
            --commit-message "Sync prebuilt leaderboard (${GITHUB_SHA::7})"
